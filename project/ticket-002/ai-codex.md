---
participant-id: agent:codex
participant: codex
role: agent
ticket: ticket-002
---
# Participant: codex (AI agent)

## Understanding

Diagit demonstrates stable, target-qualified findings, deterministic bounded
queries, ownership-boundary deduplication and inert plans that require external
authority. Doctor demonstrates secret-free, content-addressed evidence behind
canonical hash-chained operational events and versioned error runbooks. The
agent standard should capture those boundaries without copying either domain
schema into `wellmanifest.agent/v1`.

## Execution plan

1. Verify the exact Diagit and Doctor revisions and their relevant tests.
2. Clarify control-plane observation versus product-only repair in executable
   conformance.
3. Add normative finding, read-model, plan and operational-evidence rules to
   the architecture and logic-flow guidance.
4. Run conformance, Docker, governance, diff and artifact-registry checks.

## Actual changes

- Initialized the bounded ticket and recorded SESSION_EXECUTION_AUTHORIZATION
  from the request to execute this work.
- Verified Diagit `b6958c40598adcc44dd02e184bc7dc92325a90b9`
  with 109 tests, Ruff and mypy in a detached read-only worktree.
- Reused the previously verified Doctor operational-log implementation at
  `1aed648d7c45588cf1e04d95ce315b64251c80bd`.
- Limited product-only targeting to `repair`; conformance now accepts
  control-plane diagnosis and exact-SHA validation while rejecting
  control-plane repair.
- Added normative finding, fleet-query, shared-state, plan, operational-event
  and immutable-evidence rules without changing the v1 schema or grammar.
- Passed local and rebuilt networkless Docker conformance, governance, diff
  hygiene and Subactor artifact-registry build/check.
- Distilled the verified credential path into a metadata-only inventory,
  controller-owned just-in-time grant and exact-reference resolver boundary.
- Required runtime-only grant injection, success/failure revocation and
  fail-closed issuance/revocation proof without changing the v1 document
  family.

## Blockers

- None inside the recorded intent; proceed without a second confirmation.
- New authority remains required for destructive action, secret access, new
  external coordination, material objective expansion and trusted merge.
