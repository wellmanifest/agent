---
participant-id: agent:codex
participant: codex
role: agent
ticket: ticket-007
---
# Participant: codex (AI agent)

## Understanding

`agent/v1` correctly forbids every direct mutation except Repair pull requests.
The new Skills initial-ref profile therefore exposes `domain-executor` as an
unbound slot. The safe composition is a separate domain-controller profile,
not a new Repair capability and not a change to the existing version.

## Execution plan

1. Add a closed repository-publisher companion schema and canonical profile.
2. Pin the exact Skills and git-lifecycle artifacts.
3. Add semantic and adversarial conformance without dependencies.
4. Document the exceptional domain-controller boundary.
5. Run both standards and governance, then use protected delivery.

## Actual changes

- Initialized the bounded ticket and recorded SESSION_EXECUTION_AUTHORIZATION
  from the request to execute this work.
- Added a closed, additive repository-publisher schema and canonical Subactor
  profile pinned to exact Skills and git-lifecycle artifacts.
- Required an unborn remote, isolated non-executing workspace, fresh
  non-inherited single-use authority, normal push and independent Validator.
- Kept publication non-terminal and prohibited force, secret storage,
  self-validation and automatic quarantine deletion.
- Added fourteen adversarial cases and documented HOME Subactor / ADOPT
  Wellmanifest ownership.
- Preserved the raw bytes of `agent.schema.json`, `conformance.py` and
  `agent.v1.gbnf`; agent/v1 behavior is unchanged.

## Blockers

- No implementation blocker remains; protected publication is in progress.
- New authority remains required for destructive action, secret access, new
  external coordination, material objective expansion and trusted merge.
